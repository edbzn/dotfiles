- name: Install various required tools
  apt:
    name:
      - git
      - terminator
      - curl
      - htop
      - vim
      - flameshot
      - jq
    update_cache: yes
    state: present
  become: yes

- name: Create work directory
  file:
    path: ~/work
    state: directory
    mode: '0755'
