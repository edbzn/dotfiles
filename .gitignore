# Ignore machine-specific configuration files
.dotfiles/**/*.local
*.local

# Ansible retry files
*.retry

# Shell history backup (optional - remove this line to track history in git)
# .dotfiles/zsh/.zsh_history.backup

# Vault password file (NEVER commit this!)
.vault_password
.vault_pass
